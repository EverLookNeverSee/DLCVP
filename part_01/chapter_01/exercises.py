# importing relevant libraries
import torch

# returning True if pytorch library is available on our local machine
torch.cuda.is_available()

# returning the version of pytorch that we've installed
torch.__version__
